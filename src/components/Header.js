import React from 'react';
import './styles/Header.css';

const Header = () => {
  return (
    <header className="HeaderContainer">
      <h1 className="HeaderTitle">MovieSearch</h1>
    </header>
  );
};

export default Header;
